# Genesys Cloud Extension Audit - Environment Variables
# Copy this file to .env and fill in your values (if using environment-based configuration)

# Genesys Cloud API Configuration
# The base URI for your Genesys Cloud region (e.g., https://api.usw2.pure.cloud)
GC_API_BASE_URI=https://api.usw2.pure.cloud

# Access token for Genesys Cloud API authentication
# Generate from: https://apps.{your-region}.pure.cloud/directory/#/admin/integrations
# Required OAuth scopes: users:readonly, telephony:readonly, telephony:admin (for patch operations)
GC_ACCESS_TOKEN=your_access_token_here

# Optional: Set to "true" to include inactive users in audits (default: false)
# GC_INCLUDE_INACTIVE_USERS=false

# Note: The MAUI application currently accepts these values via the UI.
# This file is provided for reference and potential future CLI/automation scenarios.
